<p id="notice"><%= notice %></p>

<h1>Customer Events</h1>

<table>
  <thead>
    <tr>
      <th>Customer</th>
      <th>Event</th>
      <th>Payment</th>
      <th colspan="3"></th>
    </tr>
  </thead>

  <tbody>
    <% @customer_events.each do |customer_event| %>
      <tr>
        <td><%= customer_event.customer_id %></td>
        <td><%= customer_event.event_id %></td>
        <td><%= customer_event.payment_id %></td>
        <td><%= link_to 'Show', customer_event %></td>
        <td><%= link_to 'Edit', edit_customer_event_path(customer_event) %></td>
        <td><%= link_to 'Destroy', customer_event, method: :delete, data: { confirm: 'Are you sure?' } %></td>
      </tr>
    <% end %>
  </tbody>
</table>

<br>

<%= link_to 'New Customer Event', new_customer_event_path %>
