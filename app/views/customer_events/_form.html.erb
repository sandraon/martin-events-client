<%= form_with(model: customer_event, local: true) do |form| %>
  <% if customer_event.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(customer_event.errors.count, "error") %> prohibited this customer_event from being saved:</h2>

      <ul>
      <% customer_event.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= form.label :customer_id %>
    <%= form.number_field :customer_id %>
  </div>

  <div class="field">
    <%= form.label :event_id %>
    <%= form.number_field :event_id %>
  </div>

  <div class="field">
    <%= form.label :payment_id %>
    <%= form.number_field :payment_id %>
  </div>

  <div class="actions">
    <%= form.submit %>
  </div>
<% end %>
